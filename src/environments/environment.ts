/**
 * Production environment configuration
 *
 * This file serves as the default environment configuration.
 * When building for production (ng build --configuration production),
 * Angular CLI automatically uses this file as the base.
 * The 'production' flag set to true enables production mode.
 */

export const environment = {
  production: true,
  appName: 'DentAI',
  apiUrl: '',
  version: '1.0.0'
};
