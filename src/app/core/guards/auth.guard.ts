import { CanActivateFn } from '@angular/router';

export const authGuard: CanActivateFn = (_route, _state) => {
  // Skeleton infrastructure: to be implemented with business logic later
  return true;
};
